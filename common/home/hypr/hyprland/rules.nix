{
  wayland.windowManager.hyprland.settings = {
    windowrulev2 = [
      "workspace 2, class:($browserClass)"
      "workspace 3, class:($terminalClass)"
      "workspace 4, class:(code-url-handler), title:(Visual Studio Code)"
      "float, class:($terminalBackupClass), title:($terminalBackup)"
    ];
  };
}
